"""OpenTelemetry instrumentation (DESIGN.md §18): spans + metrics for
retrieval, writes, consolidation, decay, and degradation counters.

Added in Step 6. No built-in dashboard — users wire their own OTEL backend.
"""
