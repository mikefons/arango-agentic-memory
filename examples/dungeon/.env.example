# URL of the Python memory core (FastAPI). Local docker-compose default.
# Use 127.0.0.1 (not localhost): server-side fetch may try IPv6 ::1 and hang.
CORE_URL=http://127.0.0.1:8080

# Vercel AI Gateway key — routes the DM narration + background models.
# Get one at https://vercel.com/ai-gateway (used from 3.5c-1 onward).
AI_GATEWAY_API_KEY=
