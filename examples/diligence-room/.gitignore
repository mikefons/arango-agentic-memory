# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts.timestamp

# env — never commit real values (root .gitignore also covers these)
.env.local
.env*.local

# misc
*.tsbuildinfo
.DS_Store
/.swc

# Vercel Workflows generated routes (regenerated each build)
app/.well-known/workflow/
